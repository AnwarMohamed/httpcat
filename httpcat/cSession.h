#pragma once
#include "HttpCat.h"
#include "SRDFUtils.h"

/*
class cSession
{
private:
	DWORD	ThreadId;
	HANDLE	Thread,	
			Mutex;
public:
	cSession(cHash Params);
	~cSession(void);

	CHAR*	InputBuffer,	
			OutputBuffer;
	VOID	Run();
};
*/
